{
  "name": "lqdigital-client-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "10.17.0"
  },
  "dependencies": {
    "common-tags": "^1.8.0",
    "twilio": "^3.44.0",
    "uuid": "^8.1.0"
  }
}
